server:
  port: 8091

spring:
  application:
    name: wallet
  profiles:
    active: dev
  datasource:
    url: jdbc:mysql://localhost:3306/recargapay
    username: root
    password: password
    platform: mysql
    initialization-mode: always
  jpa:
    hibernate:
      ddl-auto: update